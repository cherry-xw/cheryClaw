# 主 Agent 运行流程图

**文档创建时间：** 未知（历史文档未记录）

状态：待综合验证。T01 至 T06 已完成；T07 全部允许的自动检查通过，人工验收尚未执行。本检查点随任务提交，未要求推送。

## 目标与边界

在工作台增加默认关闭的主 Agent 运行流程图，支持实时观察、完整工具批次、资源摘要、父子派发/回传边界与长期历史回放。保留原消息树、卡片、输入与执行语义；不提供流程编辑、逐 token 追踪、子 Agent 内部图或 Hook 执行日志。

复杂度 4/5：跨端状态、异步订阅、历史事实、压缩与配置纪元组合。执行者为用户已确认的当前 Agent；需要跨模块推理、异步边界审查与自动测试能力。本轮未分配子 Agent。

## 权威入口

| 内容 | 唯一维护位置 |
| --- | --- |
| RPC、版本、状态与历史质量 | [workflow 契约](../../shared/protocol/workflow.md) |
| 观察写者、持久附注与恢复 | [后端流程观察](../../backend/service/workflow.md) |
| 节点、交互、布局与前端接线 | [运行流程图](../../frontend/runtime-diagram.md) |
| 真实界面操作与验收记录 | [最终人工手册](verify/manual-final.md) |

原 requirements/technical-design 的有效规则已迁入以上正式文档，删除计划中的重复副本；完成子计划已收口，其原文可从 Git 历史恢复。

## 批次与台账

| ID | 批次 | 任务 | 复杂度与依据 | 依赖 | 状态与结果 |
| --- | --- | --- | --- | --- | --- |
| D00 | 设计 | 需求与方案 | 3：跨领域需求收敛 | 无 | 已完成；有效规则已迁入权威专题 |
| T01 | A | 契约与事实来源 | 4：当前、历史与纪元边界 | D00 | 已完成；协议 Zod、RPC/通知注册、资源与质量字段、文档迁移；类型与隔离 schema 检查通过 |
| T02 | B | 实时观察与订阅 | 4：多窗口与执行隔离 | T01 | 已完成；共享投影、独立租约、完整批次、真实边界、归档/删除清理；隔离状态与所有权测试通过 |
| T03 | B | 历史与上下文 | 4：只读恢复与最小持久化 | T01 | 已完成；长期事实分页、指纹冲突、冻结资源、最终附注及合并；10 项后端隔离测试通过 |
| T04 | B | 工作台空间布局 | 3：双方向与卡片、输入共存 | T01 | 已完成；同源轨道预算、宿主局部样式、输入高度保留；布局纯函数与工作台回归通过 |
| T05 | C | 流程图与实时展示 | 3：节点、工具、状态统一呈现 | T02、T04 | 已完成；Vue/SVG 渲染、条件节点、Hook 位置与未知降级；前端类型和状态测试通过 |
| T06 | C | 流程回放 | 3：分页、计时与实时隔离 | T03、T05 | 已完成；确定性步进、阶段/速度/进度、串行 open、后台暂停；纯函数与假时钟测试通过 |
| T07 | D | [综合验证与用户验收](07-final-verification.md) | 4：跨端组合与原工作台回归 | T01 至 T06 | 进行中；自动检查已通过，人工待用户执行 |

## 当前恢复检查点

- 实现与全部允许的自动检查已完成，剩余仅用户人工验收；以 Git 日志定位本提交检查点。
- 基准提交：`1afd81e`。本轮开始工作区干净；当前改动均属于本任务和其验证修正，不包含其他任务变更。
- 存储只有现有资源清单 JSON 的 `resources.workflow` 和执行节点 workflow 结果附注；无新表、无 retention 变更、无历史删除。
- 观察不是执行 owner；关闭流程或断线回收不会停止任务。恢复/归档读取不创建 runtime。
- 历史分页在上界内证据发生变化时显式 CONFLICT，重新加载；旧数据缺计数、缺精确顺序时保留未知/重建说明。
- 完整前端测试发现的原有归档提示断言已按 HEAD 中既有归档契约更新，没有将产品行为改回删除。组件超额通过提取流程接线与断连提示解决，未放宽 800 行预算。
- 后端 `test/` 冻结：未修改、未运行；任务内隔离测试不访问真实数据库或 Provider。前端长期测试保留在 `web/test/agent/`。
- 下一入口：[T07](07-final-verification.md)。人工操作按手册进行，失败登记新修正任务，不直接标记总任务完成。

## 自动验证入口

| 目标 | 命令 | 当前结论 |
| --- | --- | --- |
| 两端类型 | `pnpm type-check:all` | 最终重跑已通过 |
| 后端生产构建 | `pnpm build` | 最终重跑已通过 |
| Web/Electron 静态构建 | `pnpm web:build` | 最终重跑已通过；未启动图形界面，非阻断提醒见 T07 |
| 前端全量测试 | `pnpm test:web -- --reporter=dot` | 105 个文件、582 项通过 |
| 后端隔离事实与持久合并 | `pnpm exec vitest run --config docs/plan/main-agent-runtime-diagram/verify/vitest.config.ts` | 2 个文件、10 项通过 |
| 新增文档链接与差异 | `node docs/plan/main-agent-runtime-diagram/verify/check-doc-links.mjs`、`git diff --check` | 41 条新增/修改引用通过；差异无空白错误 |

新增文档及本次新增引用做链接检查，不以本任务修复计划总入口中其他任务的历史缺失链接。后端隔离 fixture 暂留任务 verify；测试目录限制解除后再评估迁入正式后端测试。

## 审批与提交边界

用户要求完成后提交；提交不表示人工验收已通过，不推送远端。本任务保持“待综合验证”。人工验收通过后进入“待用户审批”，用户明确批准后按[项目计划政策](../../standards/global/project-documentation.md#2-计划政策)完成总任务收口。

人工范围包括真实窗口、主题、滚动、遮挡、卡片/树相机保持、键盘与实机流程。未经额外授权不操作浏览器/Electron、不截图、不通过程序化 DOM/CSS 代替 UI 验收。
