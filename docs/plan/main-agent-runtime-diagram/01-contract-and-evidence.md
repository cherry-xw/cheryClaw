# T01 契约与事实来源

所属总任务：[主 Agent 运行流程图](README.md)。状态：未开始。批次：A。复杂度：4（较高），因为需要把现有事实映射为跨端状态并区分压缩、纪元和观察生命周期。依赖：D00。

## 范围与交付

交付 workflow API/schema、前后端共用最小类型、历史事实与资源摘要的确定来源，以及受影响权威文档。目标规则分别见[需求说明](requirements.md)与[技术设计](technical-design.md)，本子任务不重新定义字段或产品状态。

不实现图形、不改变 Agent 执行策略、不引入追踪表。共享类型归 `packages/protocol/src/`，不在前端和 service 各维护一套枚举。

## 已完成事实

- [x] 已核对现有中间件链、工具批次、压缩裁剪、提示词计数和执行节点附注机制。
- [x] 已确定 Hook 仅为挂载标记、实时与回放分离、当前流程图默认关闭。
- [ ] 权威文档迁移、schema 和映射实现尚未开始。

## 剩余步骤

- [ ] 从任务入口恢复，检查协议与归档相关并行工作区变更，记录本次实际基准。
- [ ] 按技术设计第 10 节建立/更新权威协议、状态边界和前端专题，同步最近索引；从计划正文移除已迁移的重复契约，改为链接。
- [ ] 在 [protocol 源码入口](../../../packages/protocol/src/index.ts) 所属包定义 open/close/history 和 workflow.updated 的最小类型/schema；接入 [RPC 目录](../../../packages/protocol/src/rpc.ts)。
- [ ] 同步 [service 消息模块](../../../src/service/message/) 的方法、请求/响应及通知验证绑定，前端通过既有协议绑定消费。
- [ ] 为每个字段确定具体写者：业务状态取现有执行/等待事实；压缩阶段取实际采用摘要；资源计数取真实冻结构建；加载技能取最终结果。
- [ ] 确定结果附注在现有执行节点 JSON 上的合并规则，以及当前生效上下文与历史执行事实的区分。
- [ ] 固定兼容规则：新字段可选；未知不写 0；旧数据没有重试/Hook/压缩生效证据时降级；没有主 Agent 身份时返回明确错误。
- [ ] 在本目录 `verify/` 建立当前允许的隔离契约验证入口，回写实际命令与 fixture 位置，不修改冻结的 `test/`。

## 定向自动验证

- `pnpm type-check:all`：新增类型在后端、协议包和前端消费端一致。
- 隔离 schema 用例：非法节点 ID、负计数、错误订阅标识、错误历史 cursor、缺失可选旧字段的处理符合契约。
- 状态映射用例：等待不变暂停；配置纪元不当压缩阶段；工具结果/派发成功不等于主任务完成。
- 文档本地链接与重复事实 owner 检查；不运行浏览器或程序化 UI 验收。

## 完成标准

T02 至 T06 可以直接使用一份已确定的类型与事实映射；不再需要自行猜测状态含义或字段来源。权威说明先于实现更新，自动检查结论已记录。将实际协议入口、校验命令与后续依赖回写总任务后删除本文件。
