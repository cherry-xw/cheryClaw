# T07 综合验证与用户验收

**文档创建时间：** 2026-09-10T01:19:00+08:00

状态：进行中。所属总任务：[主 Agent 运行流程图](README.md)。复杂度：4/5，跨端异步与原工作台回归组合。依赖：T01 至 T06 已完成并删除独立子计划。

## 范围与已完成事实

实现、契约与文档迁移已完成；本任务仅记录综合检查、提交和用户人工验收。无浏览器/Electron 操控，无图片或程序化 DOM/CSS 验收。后端冻结 test/ 不在执行范围。

## 自动验证

检查日期：2026-09-10。每个命令以退出码 0 为通过标准；测试同时记录数量。最后一轮代码上执行，不沿用历史失败数量豁免。

| 状态 | 目标与命令 | 结果 |
| --- | --- | --- |
| 已通过 | 两端类型：`pnpm type-check:all` | 退出码 0 |
| 已通过 | 后端：`pnpm build` | 退出码 0，SSR 构建成功 |
| 已通过 | Web/Electron 静态构建：`pnpm web:build` | 退出码 0；Web、main/preload 构建成功，未启动界面 |
| 已通过 | 前端：`pnpm test:web -- --reporter=dot` | 105 文件、582 项 |
| 已通过 | 后端隔离：`pnpm exec vitest run --config docs/plan/main-agent-runtime-diagram/verify/vitest.config.ts` | 2 文件、10 项；纯 fixture，无真实数据库 |
| 已通过 | 新文档与新增引用：`node docs/plan/main-agent-runtime-diagram/verify/check-doc-links.mjs` | 41 条新增/修改链接与锚点通过 |
| 已通过 | 差异：`git diff --check`、提交范围与 SQL 审查 | 无空白错误、无追踪表/保留期/历史删除变更；冻结 test/ 无差异 |

构建非阻断提示：Web 工作台 chunk 约 564.5 kB，触发 500 kB 提醒；构建同时给出插件耗时提示。未扩大任务去重构整个工作台打包。前端首轮两项失败已经修复并全量重跑：SFC 大小通过提取宿主接线/断连组件解决，旧删除提示断言按 HEAD 已有归档契约修正。

## 人工清单

全部由用户按[人工手册](verify/manual-final.md)执行，当前均为未执行：

- [ ] 横向上下布局、竖向三栏、小窗/低窗、输入保留区、独立滚动与树相机保持。
- [ ] 模型、完整批次、审批/回答/子任务等待、暂停/恢复、重试、Hook、资源与压缩的实机状态。
- [ ] 多窗口、断线、目标切换、归档只读、后台/最小化、关闭与重新打开。
- [ ] 当前/旧阶段回放、步进、速度、进度与实时并存，不触发真实执行。
- [ ] 浅深主题、可读文字、连线、键盘、焦点及减少动效。

每项记录执行人、日期、环境与尺寸、通过/失败及证据。所有人工检查通过之前保持总任务“待综合验证”，不能因提交完成而视为已验收。

## 剩余步骤与完成标准

- [x] 回写全部最终自动检查结论；本检查点随任务提交，不推送（提交号见 Git 日志）。
- [ ] 用户执行并记录人工清单；失败时登记修正任务。
- [ ] 全部通过后删除本子计划，总任务进入待用户审批；未通过不删除。
