# 会话归档管理

状态：待综合验证。目标是在设置「归档」中按主子会话管理只读历史，普通删除改归档，仅主会话支持整组永久删除。

## 台账与依赖

| 小任务 | 状态 | 结果或入口 |
| --- | --- | --- |
| 归档协议、数据与清理 | 已完成 | 统一父子 Agent 与任务分支关联集合；归档保留历史，永久删除失败可重试；定向数据库验证通过 |
| 设置页与跨窗口同步 | 已完成 | 设置一级 Tab、筛选分页、当前页批量、只读查看与生命周期通知已接入；前端定向测试通过 |
| 综合验证与用户验收 | 进行中 | [verification.md](verification.md) |

## 恢复检查点

实现与自动代码验证已完成，下一步由用户按 [人工验收手册](verify/manual-final.md) 检查浏览器与 Electron 的真实布局、跨窗口同步和只读交互。当前提交允许先落库，人工验收未完成前仍保持“待综合验证”。

## 验证与审批

2026-09-09 已通过前后端类型检查、归档定向数据库验证、前端相关测试、定向静态检查和两端构建。后端未修改或运行暂缓的 `test/`。构建存在既有大 chunk 提示，后端原生模块复制曾因文件占用保留根副本，但命令成功。实际浏览器/Electron 布局与交互仍由用户最终验收；通过后进入待用户审批。
