# S07 工作区准备状态组件

**所属总任务：** [万象台](README.md)  
**状态：** 未开始  
**复杂度：** 简单，快速执行 Agent  
**批次：** C  
**步骤进度：** 0/4  
**依赖：** [C15](15-C15-desktop-foundation.md)  
**交接对象：** C17

## 目标

展示队列、准备、容量不足与失败的原因和操作入口。

## 输入与契约

- C15-view-v1 工作区状态与事件契约
- 必须使用前置任务已经交付的具体版本，不能根据字段名猜测尚未完成的协议。
- 后续发现契约变化先报告 C17，不自行改动共享类型。

## 允许修改的范围

- `web/src/features/wanxiangtai/components/WorkspaceStatus.vue 及专属样式`
- `web/test/wanxiangtai/workspace-status.test.ts`
- 上述新路径为本任务专属目标；若前置契约指定了同职责等价路径，由总负责人记录映射后采用，不创建重复入口。

## 范围与非目标

- 展示 requested/queued/preparing/ready/failed 等既定状态、预计空间、使用量和等待原因。
- 调整预算、取消、重试仅 emit 约定事件；按钮按上游给出的可执行性禁用并说明原因。
- 不测算磁盘、不排序调度队列、不清理目录、不决定跳过验证或资源审批。
- 禁止修改数据库、公共协议、权限、业务状态机、RPC 注册和应用装配；禁止直接编辑总进度。
- 只实现明确完整产物，不新建只有透传用途的 container，不额外安装依赖。

## 执行步骤

- [ ] 1. 阅读指定输入与前置交接，记录契约版本，列出本任务产物和样例状态；缺失时报告交接对象。
- [ ] 2. 按范围完成「工作区准备状态组件」，满足上方逐条内容要求，保持可独立消费。
- [ ] 3. 执行下方局部检查，修正文案、类型、样例或展示问题；业务契约问题升级给 C17。
- [ ] 4. 记录实际修改文件、检查结果与已知限制，交给 C17 复核并由总负责人同步总进度。

## 定向测试与检查

- 容量不足、准备失败、不可取消、缺失数字和事件目标检查。
- 前端类型、样式和可访问性检查。

```text
pnpm exec vitest run --config web/vitest.config.ts web/test/wanxiangtai/workspace-status.test.ts
pnpm web:type-check
```
测试入口是本任务将创建的文件，目前尚未存在。展示组件测试关注用户可见状态、键盘和事件目标，不重写业务判断。

## 完成标准

- 用户能够理解为何未执行和可采取的操作。
- 所有允许范围外变更均为零；四步有记录。
- 完成不等于整体前端或功能完成，由复杂任务接入真实业务后统一验收。

## 可直接派发给 Agent 的指令

> 执行 S07，先阅读本文件及前置契约。只修改“允许修改的范围”。按四步完成产物与局部检查，记录修改文件、结果和限制。发现契约、权限或业务规则缺失时交给 C17，不得自行补业务规则。不要提交 Git，不修改总计划，返回交接摘要。


## 协作与变更边界

- 执行前核对前置任务已完成且契约版本一致；当前状态“未开始”，未解锁不等于发生故障。
- 本次落盘不是实现批准或验收。遵循总文档的已确认需求；新增技术方案以“补全建议”提供，执行时不得冒充用户原话。
- 先更新对应模块文档，再实现代码。不得修改本总计划以外无关需求、不得代用户审批。
- 共享文件由总负责人分配；遇到权限、状态或契约变更需求提交给核心负责人，不同时覆盖他人文件。
- 每完成一个步骤更新本任务；由总负责人同步总计划的步骤统计，避免多 Agent 同写总文件。
- 最终回归发现问题时重新打开本任务，不能保留虚假的“已完成”。

## 执行记录

| 项目 | 记录 |
|---|---|
| 执行 Agent | 未分配 |
| 开始/结束时间 | 未开始 |
| 采用契约版本 | 未接收 |
| 实际修改文件 | 无 |
| 定向检查结果 | 未执行 |
| 未解决问题 | 无已执行发现；前置条件见依赖 |
| 交接摘要 | 未交接 |
