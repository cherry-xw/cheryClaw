# S10 用户帮助与验收说明

**所属总任务：** [万象台](README.md)  
**状态：** 未开始  
**复杂度：** 简单，快速执行 Agent  
**批次：** E  
**步骤进度：** 0/4  
**依赖：** [C17](17-C17-interaction-panels.md)  
**交接对象：** C18／用户

## 目标

把已落地行为整理成首次用户可以完成的操作说明。

## 输入与契约

- 已完成的 C02～C17 行为与接口
- 项目交互可理解性要求
- 必须使用前置任务已经交付的具体版本，不能根据字段名猜测尚未完成的协议。
- 后续发现契约变化先报告 C18／用户，不自行改动共享类型。

## 允许修改的范围

- `docs/web/wanxiangtai-guide.md`
- `本任务文档中的人工验收说明`
- 上述新路径为本任务专属目标；若前置契约指定了同职责等价路径，由总负责人记录映射后采用，不创建重复入口。

## 范围与非目标

- 说明创建任务、项目路径、快照范围、实例准备、会议、待办、基线更新、交付/回写和结束。
- 明确休眠/归档/退役、模型压缩/实例接替、集成通过/用户回写确认之间的区别。
- 不把未实现的优化写成已有能力，不变更产品行为、不代用户完成视觉验收。
- 禁止修改数据库、公共协议、权限、业务状态机、RPC 注册和应用装配；禁止直接编辑总进度。
- 只实现明确完整产物，不新建只有透传用途的 container，不额外安装依赖。

## 执行步骤

- [ ] 1. 阅读指定输入与前置交接，记录契约版本，列出本任务产物和样例状态；缺失时报告交接对象。
- [ ] 2. 按范围完成「用户帮助与验收说明」，满足上方逐条内容要求，保持可独立消费。
- [ ] 3. 执行下方局部检查，修正文案、类型、样例或展示问题；业务契约问题升级给 C18／用户。
- [ ] 4. 记录实际修改文件、检查结果与已知限制，交给 C18／用户 复核并由总负责人同步总进度。

## 定向测试与检查

- 按 C17 的实际操作顺序核对全部入口和后果。
- 检查文档链接和示例，无无效按钮、虚构配置或假成功截图。

纯文档不运行产品构建。核验标题、链接、来源与流程一致性即可；视觉验收不伪造通过。

## 完成标准

- 首次用户无需猜测即可理解主要流程及常见阻塞。
- 所有允许范围外变更均为零；四步有记录。
- 完成不等于整体前端或功能完成，由复杂任务接入真实业务后统一验收。

## 可直接派发给 Agent 的指令

> 执行 S10，先阅读本文件及前置契约。只修改“允许修改的范围”。按四步完成产物与局部检查，记录修改文件、结果和限制。发现契约、权限或业务规则缺失时交给 C18／用户，不得自行补业务规则。不要提交 Git，不修改总计划，返回交接摘要。


## 协作与变更边界

- 执行前核对前置任务已完成且契约版本一致；当前状态“未开始”，未解锁不等于发生故障。
- 本次落盘不是实现批准或验收。遵循总文档的已确认需求；新增技术方案以“补全建议”提供，执行时不得冒充用户原话。
- 先更新对应模块文档，再实现代码。不得修改本总计划以外无关需求、不得代用户审批。
- 共享文件由总负责人分配；遇到权限、状态或契约变更需求提交给核心负责人，不同时覆盖他人文件。
- 每完成一个步骤更新本任务；由总负责人同步总计划的步骤统计，避免多 Agent 同写总文件。
- 最终回归发现问题时重新打开本任务，不能保留虚假的“已完成”。

## 执行记录

| 项目 | 记录 |
|---|---|
| 执行 Agent | 未分配 |
| 开始/结束时间 | 未开始 |
| 采用契约版本 | 未接收 |
| 实际修改文件 | 无 |
| 定向检查结果 | 未执行 |
| 未解决问题 | 无已执行发现；前置条件见依赖 |
| 交接摘要 | 未交接 |
