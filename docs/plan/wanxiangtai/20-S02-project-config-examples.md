# S02 项目配置示例

**所属总任务：** [万象台](README.md)  
**状态：** 未开始  
**复杂度：** 简单，快速执行 Agent  
**批次：** A  
**步骤进度：** 0/4  
**依赖：** [C02](02-C02-project-config.md)  
**交接对象：** C02／C15

## 目标

交付可直接通过既有校验器的 Git、普通目录、大型项目配置示例。

## 输入与契约

- C02-v1 项目配置契约及校验器
- 必须使用前置任务已经交付的具体版本，不能根据字段名猜测尚未完成的协议。
- 后续发现契约变化先报告 C02／C15，不自行改动共享类型。

## 允许修改的范围

- `test/fixtures/wanxiangtai/projects/ 下三份配置与说明`
- 上述新路径为本任务专属目标；若前置契约指定了同职责等价路径，由总负责人记录映射后采用，不创建重复入口。

## 范围与非目标

- 按 C02 已冻结格式填写来源、排除、验证命令和预算；使用虚构路径，不包含凭据。
- 说明哪些是必需配置和可调整示例，显示大型项目缩小范围对验证的影响。
- 不实现解析器、不改变默认值、不修改真实用户 project.yaml 或系统 .chery。
- 禁止修改数据库、公共协议、权限、业务状态机、RPC 注册和应用装配；禁止直接编辑总进度。
- 只实现明确完整产物，不新建只有透传用途的 container，不额外安装依赖。

## 执行步骤

- [ ] 1. 阅读指定输入与前置交接，记录契约版本，列出本任务产物和样例状态；缺失时报告交接对象。
- [ ] 2. 按范围完成「项目配置示例」，满足上方逐条内容要求，保持可独立消费。
- [ ] 3. 执行下方局部检查，修正文案、类型、样例或展示问题；业务契约问题升级给 C02／C15。
- [ ] 4. 记录实际修改文件、检查结果与已知限制，交给 C02／C15 复核并由总负责人同步总进度。

## 定向测试与检查

- 使用 C02 项目配置测试加载全部示例并校验。
- 核对三份示例均明确 source、validation 和预算；无环境密钥。

执行 `pnpm exec vitest run test/wanxiangtai/project-config.test.ts`，在 C02 校验测试内读取样例；如需要增加样例测试，由 C02 负责人接纳，S02 不修改解析器。

## 完成标准

- 三类样例均可被正式校验器接受。
- 说明与运行规则一致，无隐藏新增规则。
- 所有允许范围外变更均为零；四步有记录。
- 完成不等于整体前端或功能完成，由复杂任务接入真实业务后统一验收。

## 可直接派发给 Agent 的指令

> 执行 S02，先阅读本文件及前置契约。只修改“允许修改的范围”。按四步完成产物与局部检查，记录修改文件、结果和限制。发现契约、权限或业务规则缺失时交给 C02／C15，不得自行补业务规则。不要提交 Git，不修改总计划，返回交接摘要。


## 协作与变更边界

- 执行前核对前置任务已完成且契约版本一致；当前状态“未开始”，未解锁不等于发生故障。
- 本次落盘不是实现批准或验收。遵循总文档的已确认需求；新增技术方案以“补全建议”提供，执行时不得冒充用户原话。
- 先更新对应模块文档，再实现代码。不得修改本总计划以外无关需求、不得代用户审批。
- 共享文件由总负责人分配；遇到权限、状态或契约变更需求提交给核心负责人，不同时覆盖他人文件。
- 每完成一个步骤更新本任务；由总负责人同步总计划的步骤统计，避免多 Agent 同写总文件。
- 最终回归发现问题时重新打开本任务，不能保留虚假的“已完成”。

## 执行记录

| 项目 | 记录 |
|---|---|
| 执行 Agent | 未分配 |
| 开始/结束时间 | 未开始 |
| 采用契约版本 | 未接收 |
| 实际修改文件 | 无 |
| 定向检查结果 | 未执行 |
| 未解决问题 | 无已执行发现；前置条件见依赖 |
| 交接摘要 | 未交接 |
