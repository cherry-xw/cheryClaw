# C16 场景布局、人物移动与皮肤运行

**所属总任务：** [万象台](README.md)  
**状态：** 未开始  
**复杂度：** 复杂，核心实现 Agent  
**批次：** C  
**步骤进度：** 0/4  
**依赖：** [C15](15-C15-desktop-foundation.md)、[S04](22-S04-skin-vocabularies.md)

## 目标

实现数据驱动场景及与执行状态解耦的人物运动。

## 输入材料与事实核对

先读[完整设计记录](design-and-history.md) §2～§10 的有效方案，再读下列入口。标为前置产物的文件由前置任务创建；其他列出的入口用于定位现有实现，不代表本任务必须修改所有文件。

- `web/src/features/pets/components/PetSprite.vue`
- `web/src/features/pets/components/PetBody.vue`
- `docs/web/pet/movement.md`
- `docs/standards/ui-visual-and-interaction.md`

## 范围与非目标

允许承担的修改范围：

- web/src/features/wanxiangtai/ 场景模型、布局、运动和渲染控制
- 皮肤与布局配置的持久化接线
- web/test/wanxiangtai/ 场景测试

非目标：不顺手重构无关功能、不提前实现下游展示、不改用户项目原始文件来验证计划。具体物理文件可遵循仓库归属规范拆分，但若扩大共享写入范围，先由总负责人重新分配。不得将任务文档内算法建议升级为全局规范；需要形成规范时落入 docs/standards/。

## 执行步骤

- [ ] 1. 冻结 C16-scene-v1 模板格式、语义区域和校验器，提供一个最小合法模板供测试与 S05 填充。
- [ ] 2. 实现工位锚点、区域内相对位置、尺寸变化重排、拖拽和恢复默认布局；不让人物显示名称承担定位主键。
- [ ] 3. 接入 S04 词表，实现 DOM 人物＋Canvas 氛围、小组聚拢/正式会议移动/归位；减少动态效果与不可见桌面释放动画，业务不等待动画。
- [ ] 4. 用最小模板验证同时会议、拖拽后归位、任务换皮肤、旧版本皮肤恢复、长名称、缩小窗口与后台资源释放；交 S05 完成四类模板数据。

## 定向测试

- 旧任务保存其皮肤版本和专名覆盖，不随全局默认漂移。
- 不复制 pet 疲劳等装饰状态为实例休眠条件。
- 组件移除和桌面不可见时事件/动画订阅得到正确释放。

新增定向测试入口：`web/test/wanxiangtai/scenes.test.ts`。使用现有 Vitest，不另引入测试框架。该文件为实施目标，目前尚未创建。

```text
pnpm exec vitest run --config web/vitest.config.ts web/test/wanxiangtai/scenes.test.ts
pnpm web:type-check
```



定向测试必须验证对外行为及失败后果，不只复写实现细节。全量检查由批次与最终回归统一记录，不把局部通过当综合通过。

## 完成标准

- 模板格式和运行引擎独立，可在 S05 未完成前验证引擎。
- 四步均已完成，定向验证有真实结果；产物与模块文档一致。
- 交接实际文件、接口/数据版本、运行命令、已知限制和受影响下游。
- 提供 C16-scene-v1 及最小合法模板；S05 只填数据，C16 不反向等待 S05。

## 协作与变更边界

- 执行前核对前置任务已完成且契约版本一致；当前状态“未开始”，未解锁不等于发生故障。
- 本次落盘不是实现批准或验收。遵循总文档的已确认需求；新增技术方案以“补全建议”提供，执行时不得冒充用户原话。
- 先更新对应模块文档，再实现代码。不得修改本总计划以外无关需求、不得代用户审批。
- 共享文件由总负责人分配；遇到权限、状态或契约变更需求提交给核心负责人，不同时覆盖他人文件。
- 每完成一个步骤更新本任务；由总负责人同步总计划的步骤统计，避免多 Agent 同写总文件。
- 最终回归发现问题时重新打开本任务，不能保留虚假的“已完成”。

## 执行记录

| 项目 | 记录 |
|---|---|
| 执行 Agent | 未分配 |
| 开始/结束时间 | 未开始 |
| 采用契约版本 | 未接收 |
| 实际修改文件 | 无 |
| 定向检查结果 | 未执行 |
| 未解决问题 | 无已执行发现；前置条件见依赖 |
| 交接摘要 | 未交接 |
