# C01 领域契约、协议与数据归属

**所属总任务：** [万象台](README.md)
**状态：** 未开始
**复杂度：** 复杂，核心实现 Agent
**批次：** A
**步骤进度：** 0/4
**依赖：** 无；可立即开始

## 目标

冻结所有协作对象的身份、权限和状态事实，给下游提供唯一契约。

## 输入材料与事实核对

先读[完整设计记录](design-and-history.md) §2～§10 的有效方案，再读下列入口。标为前置产物的文件由前置任务创建；其他列出的入口用于定位现有实现，不代表本任务必须修改所有文件。

- `packages/protocol/src/chat.ts`
- `src/db/index.ts`
- `src/db/conversationBranch.ts`
- `src/service/chat/handler.ts`
- `docs/context-epochs.md`

## 范围与非目标

允许承担的修改范围：

- packages/protocol/src/ 下的万象台契约及公开导出
- src/db/ 下的万象台持久模型与迁移
- docs/ 下相应领域与协议说明

非目标：不顺手重构无关功能、不提前实现下游展示、不改用户项目原始文件来验证计划。具体物理文件可遵循仓库归属规范拆分，但若扩大共享写入范围，先由总负责人重新分配。不得将任务文档内算法建议升级为全局规范；需要形成规范时落入 docs/standards/。

## 执行步骤

- [ ] 1. 定义 taskId/rootChatId/teamId/instanceId/sourceId/snapshotId/discussionId/topicId/deliveryId/operationId，明确已有 conversation task 与 spawn task 的区别；更新模块文档和权限矩阵。
- [ ] 2. 建立任务、团队、实例、议题、讨论、提案、投递、工作区及操作记录；确定数据库事实源、文件清单事实源和只读投影，登记唯一约束与版本字段。
- [ ] 3. 实现共享请求/响应/事件类型、命令幂等和期望版本检查；冻结 C01-v1 契约，提供状态字典、错误码和供简单任务使用的最小类型样例。
- [ ] 4. 运行迁移与协议定向测试，覆盖旧库升级、重复操作 ID、同 ID 不同参数、版本过期、伪造调用者、旧任务读写；记录契约版本与下游交接。

## 定向测试

- 升级空库与旧库后旧任务仍可读取；重复执行迁移无副作用。
- 命令重试只产生一次业务效果；过期对象和伪造身份明确失败。
- 新通知限定为万象台能力，旧工作台不被未知事件破坏。

新增定向测试入口：`test/wanxiangtai/contracts.test.ts`。使用现有 Vitest，不另引入测试框架。该文件为实施目标，目前尚未创建。

```text
pnpm exec vitest run test/wanxiangtai/contracts.test.ts
pnpm type-check
```

协议变更还需运行 `pnpm test:protocol`。

定向测试必须验证对外行为及失败后果，不只复写实现细节。全量检查由批次与最终回归统一记录，不把局部通过当综合通过。

## 完成标准

- 身份、权限、状态、持久化归属均有明确说明。
- 契约可被前后端和简单任务共同使用。
- 四步均已完成，定向验证有真实结果；产物与模块文档一致。
- 交接实际文件、接口/数据版本、运行命令、已知限制和受影响下游。
- 提供 C01-v1 类型与状态字典，S03/S04 可直接消费。

## 协作与变更边界

- 执行前核对前置任务已完成且契约版本一致；当前状态“未开始”，未解锁不等于发生故障。
- 本次落盘不是实现批准或验收。遵循总文档的已确认需求；新增技术方案以“补全建议”提供，执行时不得冒充用户原话。
- 先更新对应模块文档，再实现代码。不得修改本总计划以外无关需求、不得代用户审批。
- 共享文件由总负责人分配；遇到权限、状态或契约变更需求提交给核心负责人，不同时覆盖他人文件。
- 每完成一个步骤更新本任务；由总负责人同步总计划的步骤统计，避免多 Agent 同写总文件。
- 最终回归发现问题时重新打开本任务，不能保留虚假的“已完成”。

## 执行记录

| 项目          | 记录                         |
| ------------- | ---------------------------- |
| 执行 Agent    | 未分配                       |
| 开始/结束时间 | 未开始                       |
| 采用契约版本  | 未接收                       |
| 实际修改文件  | 无                           |
| 定向检查结果  | 未执行                       |
| 未解决问题    | 无已执行发现；前置条件见依赖 |
| 交接摘要      | 未交接                       |
