# S03 测试样例数据

**所属总任务：** [万象台](README.md)
**状态：** 未开始
**复杂度：** 简单，快速执行 Agent
**批次：** A
**步骤进度：** 0/4
**依赖：** [C01](01-C01-contracts.md)
**交接对象：** C06／C15／C18

## 目标

提供可供 UI 与定向测试使用的稳定普通数据。

## 输入与契约

- C01-v1 协议与状态字典
- 必须使用前置任务已经交付的具体版本，不能根据字段名猜测尚未完成的协议。
- 后续发现契约变化先报告 C06／C15／C18，不自行改动共享类型。

## 允许修改的范围

- `test/fixtures/wanxiangtai/ 的后端样例`
- `web/test/fixtures/wanxiangtai/ 的前端样例`
- 上述新路径为本任务专属目标；若前置契约指定了同职责等价路径，由总负责人记录映射后采用，不创建重复入口。

## 范围与非目标

- 覆盖团队/实例、工作区、会议、提案、待办和交付的正常、等待、失败、空列表、长文本及过期版本。
- 固定 ID 与时间，确保状态关系合法；不把随机数据引入断言。
- 只生产 fixture；不修改类型、业务状态机或编写 mock provider 调度逻辑。
- 禁止修改数据库、公共协议、权限、业务状态机、RPC 注册和应用装配；禁止直接编辑总进度。
- 只实现明确完整产物，不新建只有透传用途的 container，不额外安装依赖。

## 执行步骤

- [ ] 1. 阅读指定输入与前置交接，记录契约版本，列出本任务产物和样例状态；缺失时报告交接对象。
- [ ] 2. 按范围完成「测试样例数据」，满足上方逐条内容要求，保持可独立消费。
- [ ] 3. 执行下方局部检查，修正文案、类型、样例或展示问题；业务契约问题升级给 C06／C15／C18。
- [ ] 4. 记录实际修改文件、检查结果与已知限制，交给 C06／C15／C18 复核并由总负责人同步总进度。

## 定向测试与检查

- 类型检查与正式 schema 校验通过。
- 确认失败、未验证、过期对象没有伪造成功字段。

执行 `pnpm type-check` 和 `pnpm web:type-check`；类型未覆盖 fixture 时由 C01 提供既有 schema 检查入口，禁止以“文件未纳入编译”冒充通过。

## 完成标准

- 样例可直接用于 C15 基础界面及核心任务测试。
- 所有允许范围外变更均为零；四步有记录。
- 完成不等于整体前端或功能完成，由复杂任务接入真实业务后统一验收。

## 可直接派发给 Agent 的指令

> 执行 S03，先阅读本文件及前置契约。只修改“允许修改的范围”。按四步完成产物与局部检查，记录修改文件、结果和限制。发现契约、权限或业务规则缺失时交给 C06／C15／C18，不得自行补业务规则。不要提交 Git，不修改总计划，返回交接摘要。

## 协作与变更边界

- 执行前核对前置任务已完成且契约版本一致；当前状态“未开始”，未解锁不等于发生故障。
- 本次落盘不是实现批准或验收。遵循总文档的已确认需求；新增技术方案以“补全建议”提供，执行时不得冒充用户原话。
- 先更新对应模块文档，再实现代码。不得修改本总计划以外无关需求、不得代用户审批。
- 共享文件由总负责人分配；遇到权限、状态或契约变更需求提交给核心负责人，不同时覆盖他人文件。
- 每完成一个步骤更新本任务；由总负责人同步总计划的步骤统计，避免多 Agent 同写总文件。
- 最终回归发现问题时重新打开本任务，不能保留虚假的“已完成”。

## 执行记录

| 项目          | 记录                         |
| ------------- | ---------------------------- |
| 执行 Agent    | 未分配                       |
| 开始/结束时间 | 未开始                       |
| 采用契约版本  | 未接收                       |
| 实际修改文件  | 无                           |
| 定向检查结果  | 未执行                       |
| 未解决问题    | 无已执行发现；前置条件见依赖 |
| 交接摘要      | 未交接                       |
