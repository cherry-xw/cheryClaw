# 最终验证范围（待 C18 启用）

所属任务：[万象台](../README.md)。这是待实施的验证要求，不是已进入执行的子计划。其余 27 项完成并删除后，再据此创建 C18，登记实际命令、范围、通过标准和执行记录。当前全部未执行。

## 目标

确认核心闭环、简单产物和双端集成达到统一验收标准，并集中承担全部需要用户操作、主观判断或真实界面确认的事项。

## 范围与非目标

允许承担的修改范围：

- test/wanxiangtai/ 和 web/test/wanxiangtai/ 端到端场景
- 必要的回归修复（须登记新的修正小任务）
- 总计划验收证据与模块验收说明

非目标：不顺手重构无关功能、不提前实现下游展示、不改用户项目原始文件来验证计划。具体物理文件可遵循仓库归属规范拆分，但若扩大共享写入范围，先由总负责人重新分配。不得将任务文档内算法建议升级为全局规范；需要形成规范时落入 docs/standards/。

## 执行步骤

- [ ] 1. 建立可确定回放的产品/UI/开发团队任务，覆盖协作、并行实例、快照版本、审批和完整交付；固定模型响应与失败注入点。
- [ ] 2. 执行后端、Web、协议测试、类型检查、构建及静态检查，分类记录已有失败与新增失败；修复时登记新的修正小任务并回归依赖。
- [ ] 3. 通过自动测试或可重复脚本执行 20 任务/50 实例投影、4 团队并行、三次压缩、大文件/大量小文件、磁盘限额、断线、重启、投递中断及回写中断验证，记录测试主机和数据规模。
- [ ] 4. 按 [最终手动验证手册](manual-final.md) 完成浏览器/Electron 流程、视觉和真实设备性能验收，归集证据；全部满足后进入待用户审批。

## 自动验证清单

- [ ] 固定响应和失败注入的全链路场景通过。
- [ ] 后端、Web、协议测试、双端类型检查、构建和静态检查通过，已有失败与新增失败分开记录。
- [ ] 规模、并发、上下文压缩、文件数据、磁盘限制、断线、重启及中断恢复验证通过。

## 手动验证清单

- [ ] 按 [最终手动验证手册](manual-final.md) 完成浏览器与 Electron 的创建、会议、澄清/审批、交付、回写和结束流程。
- [ ] 按手册完成场景布局、皮肤、长文本、多窗口和视觉可读性验收。
- [ ] 按手册记录测试设备、规模、性能观察、执行人、日期和结论。

## 定向测试

- S 系列全部通过不代替核心闭环验证。
- 并发、数据完整性、等待/权限和恢复失败必须解决。
- 当前任务中心遗留问题重新核实，不能沿用历史测试数当本次结果。

新增定向测试入口：`test/wanxiangtai/full-workflow.test.ts`。使用现有 Vitest，不另引入测试框架。该文件为实施目标，目前尚未创建。

```text
pnpm exec vitest run test/wanxiangtai/full-workflow.test.ts
pnpm type-check
```

全量入口：`pnpm test:all`、`pnpm type-check:all`、`pnpm build`、`pnpm web:build`、`pnpm lint`、`pnpm --filter web lint`。
定向测试必须验证对外行为及失败后果，不只复写实现细节。全量检查由批次与最终回归统一记录，不把局部通过当综合通过。

## 完成标准

- 自动与手动验证清单均有真实证据，未通过项目无伪完成状态。
- 四步均已完成，定向验证有真实结果；产物与模块文档一致。
- 交接实际文件、接口/数据版本、运行命令、已知限制和受影响下游。
- 失败时保留可追溯证据，不通过默认成功或忽略错误掩盖问题。


### 14.1 必测矩阵

| 场景                         | 预期                                   | 主责        |
| ---------------------------- | -------------------------------------- | ----------- |
| 创建快照后原项目变化         | 实例仍读旧基线＋自己修改               | C03/C04     |
| 快照复制期间文件变化         | 重试或失败，不发布混乱半成品           | C03         |
| 两实例修改同一路径           | 相互隔离，集成时处理冲突               | C04/C12     |
| 文件新增/删除/空/二进制/链接 | 正确交付或明确不支持，不能静默丢弃     | C03/C11     |
| 磁盘或准备并发超预算         | 队列及原因可见，失败释放预留           | C04         |
| 成员越权开会或创建子团队     | 后端拒绝，并提供向上请求路径           | C05/C09     |
| A/B交叉邀请与父等子子问父    | 无永久互等，明确协调结果               | C08         |
| 旧版本提案已同意后更新       | 同意不自动用于新版本                   | C09         |
| 连续三次压缩                 | 强约束、异议、待办、版本与消费位置保存 | C10         |
| 大工具结果/历史代际很多      | 有界读取，所有模型入口受容量检查       | C10         |
| 业务落库后投递前崩溃         | 恢复后投递并去重                       | C07/C14     |
| 自动合并成功但行为验证失败   | 不发布可采用快照                       | C12         |
| 实例有修改时采用S1           | 三方更新，失败保留原目录               | C12         |
| 用户确认后原项目再次变化     | 确认失效，重新生成候选                 | C13         |
| 回写中途失败/再次外部改动    | 恢复日志可解释，停止危险覆盖           | C13         |
| 工作中实例被新实例顶替       | 仍在台上或全局运行栏可见               | C14/C15     |
| 归档与退役/旧纪元恢复        | 只恢复有资格实例，历史不丢             | C14         |
| 多端同时处理待办             | 一个结果生效，其余端同步               | C06/C17     |
| 未处理交付或失败项仍存在     | 结束前展示并阻止虚报完成               | C14/C17     |
| 皮肤变化与不同窗口           | 状态不变、锚点恢复、字词有通用解释     | C16/S04/S05 |
| 简单组件为空/长文本/等待     | 正确显示，emit明确ID/版本              | S06～S09    |

