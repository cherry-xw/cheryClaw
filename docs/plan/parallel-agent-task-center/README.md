# 多 Agent 并行任务中心

**状态：** 待综合验证

## 目标与边界

- 为浏览器与 Electron 增加跨预设的单例任务中心，按根会话聚合并行 Agent 的轻量状态、关键事件与待办。
- 待办固化“为什么需要决定/决定后做什么”，不增加模型调用；完整节点树与协议历史继续按需加载。
- 移除工作台 1.2 秒任务时间线轮询。
- 第一版不增加任务/Agent 级暂停终止、声音设置、完整多流平铺或新的持久化任务状态表。

## 已完成结果

- [x] 完成 overview 协议、后端快照/增量投影及定向验证。
- [x] 完成待办上下文持久化、协议投影和增量同步。
- [x] 完成任务中心 store、界面、浏览器/Electron 入口及旧待操作入口整合。
- [x] 移除固定轮询并覆盖订阅获取、释放、断线恢复与 revision 缺口自愈。
- [x] 后端 1197 项、协议 436 项、双端类型检查及 Web/Electron 构建已有通过记录；当前共享工作树 Web 全量 566 项通过。

## 当前子计划

- [ ] [04 综合验证与用户验收](04-integration-regression.md)：待完成无轮询、有界增量和双端交互验收。

## 恢复检查点

- 当前只继续 04，不重做已完成实现。
- 人工操作统一按 [最终手册](verify/manual-final.md) 执行。
- 验证发现实现问题时，新建修正子计划；不得恢复已删除的旧子计划。
- 综合验证通过后删除 04，并将本计划改为 `待用户审批`；用户明确批准后标记 `已完成`。
