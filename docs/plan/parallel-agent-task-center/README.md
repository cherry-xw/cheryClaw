# 多 Agent 并行任务中心

**状态：** 待综合验证

## 目标与边界

- 为浏览器与 Electron 增加跨预设的单例任务中心，按根会话聚合并行 Agent 的轻量状态、关键事件与待办。
- 待办固化“为什么需要决定/决定后做什么”，不增加模型调用；完整节点树与协议历史继续按需加载。
- 移除工作台 1.2 秒任务时间线轮询。
- 第一版不增加任务/Agent 级暂停终止、声音设置、完整多流平铺或新的持久化任务状态表。

## 已完成结果

- [x] 完成 overview 协议、后端快照/增量投影及定向验证。
- [x] 完成待办上下文持久化、协议投影和增量同步。
- [x] 完成任务中心 store、界面、浏览器/Electron 入口及旧待操作入口整合。
- [x] 移除固定轮询并覆盖订阅获取、释放、断线恢复与 revision 缺口自愈。
- [x] 后端 1197 项、协议 436 项、双端类型检查及 Web/Electron 构建已有通过记录；历史共享工作树 Web 全量 566 项通过。

## 批次与小任务台账

| 批次 | 小任务 | 状态 | 结果或剩余工作 |
| --- | --- | --- | --- |
| A | 01–03 实现与定向回归 | 已完成 | 结果与已有验证结论见上；旧明细已删除，不重建过程记录 |
| B | [04 综合验证与用户验收](04-integration-regression.md) | 进行中 | 最终工作树自动复核及用户人工验收 |

依赖：overview 协议/投影 → 待办上下文与增量 → 前端任务中心和轮询移除，全部实现完成后进入 B。共享窗口、状态和入口修改需串行集成；当前无实施并行写入。

## 恢复检查点

- 本次整理基准：`b50f439`；历史实现和验证摘要不代表在此版本重新运行。
- 下一条验证命令：`pnpm test:protocol`。

- 当前只继续 04，不重做已完成实现。
- 人工操作统一按 [最终手册](verify/manual-final.md) 执行。
- 验证发现实现问题时，新建修正子计划；不得恢复已删除的旧子计划。
- 综合验证通过后删除 04，并将本计划改为 `待用户审批`；用户明确批准后标记 `已完成`。

## 最终综合验证与用户审批

自动和手动清单统一见 [04](04-integration-regression.md)；保留历史结果，尚未完成的最终复核不推定通过。人工结论与执行人、日期待用户填写；当前未获最终审批。
