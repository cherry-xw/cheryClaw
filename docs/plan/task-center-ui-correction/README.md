# 任务中心 UI 修正与旧待操作退役

**状态：** 待综合验证

## 目标与边界

按用户 2026-09-05 要求修正任务中心控件、间距、字号和字重；除标题外统一 400；废弃桌面及工作台旧待操作面板。保留共享交互服务、Lite 与节点树能力。沿用现有 Element Plus，不增加依赖，不扩展后端。

## 已完成结果

- [x] 完成任务中心控件、布局、间距、字号、字重、选项备注和自由回答样式修正。
- [x] 退役桌面与工作台旧待操作入口，保留共享交互服务、Lite 与节点树使用路径。
- [x] 定向测试、Web 类型检查、相关 ESLint 及 Web/Electron 构建已有通过记录；历史共享工作树 Web 全量 566 项通过。

原 01、02 已完成并删除；结果保留在本台账，不再链接已删除文档。

## 批次与小任务台账

| 批次 | 小任务 | 状态 | 结果或剩余工作 |
| --- | --- | --- | --- |
| A | 01 任务中心 UI 修正 | 已完成 | 结果与已有验证结论见上；旧明细已删除，不重建过程记录 |
| A | 02 旧待操作退役 | 已完成 | 旧入口移除，保留共享交互、Lite 和节点树；定向验证已有通过记录 |
| B | [03 综合验证与用户验收](03-final-verification.md) | 进行中 | 最终工作树自动复核及用户人工验收 |

依赖：01 UI 修正 → 02 旧待操作退役，全部实现完成后进入 B。共享窗口、状态和入口修改需串行集成；当前无实施并行写入。

## 恢复检查点

- 本次整理基准：`b50f439`；历史实现和验证摘要不代表在此版本重新运行。
- 下一条验证命令：`pnpm test:web`。

- 当前只继续 03，不重做已完成实现。
- 人工操作统一按 [最终手册](verify/manual-final.md) 执行。
- 综合验证通过后删除 03，并将本计划改为 `待用户审批`；用户明确批准后标记 `已完成`。

## 最终综合验证与用户审批

自动和手动清单统一见 [03](03-final-verification.md)；保留历史结果，尚未完成的最终复核不推定通过。人工结论与执行人、日期待用户填写；当前未获最终审批。
