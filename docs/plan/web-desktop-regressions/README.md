# Web 桌面回归修复

**状态：** 待综合验证

## 目标与边界

恢复浏览器桌面的既有契约：任务栏窗口激活、设置初始化与标签动效、Pet 移动/拖拽，以及主 Pet 的可见投影。不修改后端协议、配置 schema，也不增加新 UI 功能。

## 已完成结果

- [x] 修复工作区恢复和任务栏窗口激活路径。
- [x] 修复设置初始化、持久化可见性和标签动效。
- [x] 修复 Pet 舞台移动、拖拽和主 Pet 可见投影。
- [x] 受影响自动测试、Web 类型检查和生产构建已有通过记录。

## 批次与小任务台账

| 批次 | 小任务 | 状态 | 结果或剩余工作 |
| --- | --- | --- | --- |
| A | 01–03 实现与定向回归 | 已完成 | 结果与已有验证结论见上；旧明细已删除，不重建过程记录 |
| B | [04 综合验证与用户验收](04-integration.md) | 进行中 | 最终工作树自动复核及用户人工验收 |

依赖：工作区恢复 → 设置生命周期 → Pet 投影和交互，全部实现完成后进入 B。共享窗口、状态和入口修改需串行集成；当前无实施并行写入。

## 恢复检查点

- 本次整理基准：`b50f439`；历史实现和验证摘要不代表在此版本重新运行。
- 下一条验证命令：`pnpm test:web`。

- 当前只继续 04，不重做已完成实现。
- 人工操作统一按 [最终手册](verify/manual-final.md) 执行。
- 验证发现实现问题时，新建修正子计划；不得恢复已删除的旧子计划。
- 综合验证通过后删除 04，并将本计划改为 `待用户审批`；用户明确批准后标记 `已完成`。

## 最终综合验证与用户审批

自动和手动清单统一见 [04](04-integration.md)；保留历史结果，尚未完成的最终复核不推定通过。人工结论与执行人、日期待用户填写；当前未获最终审批。
